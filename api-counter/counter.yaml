name: demo
version: 1.0.0
interfaces:
  - name: Counter
    properties:
      - name: counter
        type: int
    operations:
      - name: increment
        params:
          - name: step
            type: int
      - name: decrement
        params:
          - name: step
            type: int
